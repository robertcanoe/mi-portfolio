version: '3.8'
services:
  astro:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "4321:4321"
    volumes:
      - ./package.json:/app/package.json
      - ./src:/app/src
    environment:
      - NODE_ENV=development
    command: pnpm run dev